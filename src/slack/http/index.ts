// Stub — Slack channel removed in security-testing edition.
import type { Request, Response, NextFunction } from "express";
export function handleSlackHttpRequest(_req: Request, _res: Response, _next: NextFunction) { _next(); }
