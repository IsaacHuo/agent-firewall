// Stub — LINE channel removed in security-testing edition.
export function stripMarkdown(text: string) { return text.replace(/[*_~`#[\]]/g, ""); }
export function markdownToLine(text: string) { return text; }
