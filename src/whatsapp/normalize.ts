// Stub — WhatsApp channel removed in security-testing edition.
export function normalizeWhatsAppTarget(raw: string) { return raw?.trim() ?? ""; }
export function isWhatsAppGroupJid(_jid: string) { return false; }
